streamlit==1.35.0
# Use TensorFlow CPU for maximum compatibility on local and server setups
tensorflow-cpu>=2.11.0,<2.22.0
# Use headless OpenCV to avoid GUI dependency issues
opencv-python-headless==4.8.1.78
# Keep numpy flexible but compatible with the selected packages
numpy>=1.24.0,<2.0.0
